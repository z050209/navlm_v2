# NavLM v2 — daily log — 2026-05-22

Running record of progress and steps for the from-scratch rebuild.

## Decisions

- New codebase **`navlm_v2`** at `C:\Users\z0502\Desktop\cs231n\navlm_v2`
  (local disk — fast video I/O, no Google Drive sync thrash).
- Build **from scratch**; old `navlm_ss` code kept only as reference.
- Code → GitHub. Datasets + checkpoints → Hugging Face. Data lives on
  local disk under a configurable `DATA_ROOT`.
- `navlm_ss` (Google Drive) stays untouched as the milestone-2 snapshot.

## Done today

- Created `navlm_v2/`; copied old code in, scrubbed hardcoded API keys
  (Maps / Gemini / Mapillary) from the 4 affected files.
- Moved all copied old code into `navlm_v2/reference/` (reference only —
  not the working codebase).
- Documented the 8 source video URLs →
  `G:\My Drive\cs231n\project\milestone2\videos\video_urls.md`.
- Launched a code-review of `navlm_ss` to document how every prior
  pipeline stage worked (video crawl, slicing, Gemma POI scan, POI lists,
  routing, VLM instruction annotation, experiments, image↔POI indexing).

- Code-review of `navlm_ss` completed — documented video crawl, frame
  slicing, Gemma POI scan, POI lists, routing, VLM instruction
  annotation, the 6-condition experiments, and image↔POI indexing.
- Wrote `DEV_MANUAL.md` — full rebuild design doc with v1-vs-v2 for every
  stage and 7 open design decisions (D-A … D-G).

## In progress

- Awaiting user sign-off on `DEV_MANUAL.md` design decisions.

## Open questions (to resolve in DEV_MANUAL)

- Scope of v2 now: Phase A (video → trusted GPS frames) first.
- Core GPS-recovery logic: DINOv2-primary vs VLM-primary vs equal-vote.
- Reference imagery: Street View only, or keep Mapillary as fallback.
- Image quality filters to add (blur / exposure / duplicate removal).
- **Train/test split policy.** Old split = hold out one whole video
  (`saturday_morning`) — but it still walks the *same* landmarks as
  training. Consider a **POI-based hold-out**: reserve some destination
  POIs entirely so the test set routes to places never seen in training
  (a real unseen-place generalization test). Two axes to decide:
  (a) held-out camera frames/video, (b) held-out destination POIs.

## Next

- Receive code-review findings → fill in `DEV_MANUAL.md`.
- Review with user before any execution.
