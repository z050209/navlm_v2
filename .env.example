# Copy to `.env` and fill in. `.env` is gitignored — never commit real keys.
# Key strings live in the GCP console (see logs/infra.md §3).

GOOGLE_MAPS_API_KEY=    # Street View Static API  (key: streetview-test)
GEMINI_API_KEY=         # Gemini / Generative Language API  (key: gemini-navlm)
MAPILLARY_TOKEN=        # only if comparing against Mapillary
HF_TOKEN=               # Hugging Face — dataset / checkpoint hosting
